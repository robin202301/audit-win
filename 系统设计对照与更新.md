# 审计系统设计对照与更新
# 系统名称：基层审计机关审计辅助系统 v0.1。下边落款：科右前旗审计局

## 一、文档来源说明

| 来源文件            | 用途 |
|-----------------|------|
| `审计流程-重新设计.md`  | 25 步阶段化设计文档（已更新） |
| `审计流程4.30.docx` | 27 步完整版业务流程（含审计底稿汇总表、审中碰头会等） |
| `各页面关系.docx`    | 页面间数据引用关系说明（11 条引用链） |
| `说明.doc`        | 业务补充说明（WPS 旧格式） |

## 二、当前实现架构总览

### 2.1 技术栈

- **主进程**: Electron + TypeScript (tsc && tsc-alias 编译)
- **渲染进程**: Vue 3 (Composition API) + Vite + Pinia + Vue Router
- **数据库**: SQLite (better-sqlite3)
- **文档处理**: docxtemplater + pizzip（模板填充）、mammoth.js（Word 解析）、xlsx（Excel 处理）
- **打包**: electron-builder → release/ 目录

### 2.2 审计类型（仅三种）

1. **经济责任审计**
2. **预算执行审计**
3. **专项审计调查**

### 2.3 项目结构

```
audit-win/
├── src/
│   ├── main/
│   │   ├── index.ts
│   │   ├── ipc/
│   │   │   ├── index.ts          # IPC 注册入口
│   │   │   └── document.ts       # 文档生成 IPC
│   │   └── services/
│   │       └── templateService.ts # 模板读取、占位符校验、文档/Excel 生成
│   ├── renderer/
│   │   ├── views/
│   │   │   ├── Home.vue              # 项目列表（含新建项目，审计类型下拉选择）
│   │   │   ├── ProjectDetail.vue     # 项目详情 + 5 阶段 Tab + 步骤列表
│   │   │   ├── StageView.vue         # 路由分发器
│   │   │   ├── StageEvidence.vue     # 取证单 1:N 管理（专用）
│   │   │   ├── StageWorkingPaper.vue # 底稿 1:N 管理（专用）
│   │   │   ├── StageArchiveCatalog.vue # 审计档案目录（专用）
│   │   │   └── stages/
│   │   │       ├── stageConfigs.ts         # 步骤表单配置（fields: [] 表示纯导出页面）
│   │   │       ├── GenericStageForm.vue    # 通用结构化表单（v-if/v-else 分离编辑/只读 DOM）
│   │   │       └── UniversalStageEditor.vue # 通用文本编辑器
│   │   └── stores/project.ts
│   ├── database/
│   │   ├── migrations.ts         # 27 阶段 trigger（含调查子步骤）
│   │   └── repositories/
│   └── shared/
│       └── types.ts              # WORKFLOW_STEPS（27 步）、AUDIT_TYPES、NOTICE_TEMPLATES
├── resources/templates/          # 模板文件（标准化命名：数字编号+中文）
└── export/                       # 导出产物
```

### 2.4 数据库表

| 表名 | 用途 |
|------|------|
| `projects` | 审计项目（name, audited_target, audit_type, status） |
| `stage_progress` | 阶段进度（27 行/项目，project_id + stage + data_json + status） |
| `evidence_items` | 取证单（独立表） |
| `working_papers` | 底稿（独立表） |
| `survey_records` | 调查记录（独立表） |
| `file_attachments` | 文件附件 |
| `evidence_working_paper_links` | 取证-底稿关联 |

## 三、27 步工作流 vs 实现状态对照

### 3.1 审计准备阶段（步骤 1-9）

| # | 文档名称                         | 模板文件                                           | 实现状态 | 数据引用 |
|---|------------------------------|------------------------------------------------|---------|---------|
| 1 | 审计通知书                        | `1经济责任审计通知书`/`2预算执行通知书`/`3专项审计调查通知书`（根据审计类型选择） | ✅ 已实现 | 无 |
| 2 | 通知附件八不准                      | `4通知附件八不准`                                     | ✅ 纯导出页 | 从通知书导入 |
| 3 | 审计文书送达回证                     | `5审计文书送达回证`                                    | ✅ 已实现 | 从通知书导入 |
| 4 | 经济责任审计公示（仅经济责任审计） | 无                                              | ✅ 已实现 | 从通知书导入 |
| 5 | 被审计单位承诺书                     | `6被审计单位承诺书`                                    | ✅ 纯导出页 | 无 |
| 6 | 被审计单位基本情况表               | `7调查了解记录1基本情况表`（Excel）                     | ✅ 已实现 | 无 |
| 6 | 评估被审计单位存在重要问题的可能性   | `8调查了解记录2评估可能性`                             | ✅ 纯导出页 | 从基本情况表导入 |
| 6 | 确定审计事项和审计应对措施           | `9调查了解记录3审计应对措施`                           | ✅ 纯导出页 | 从评估可能性导入 |
| 7 | 审计实施方案                       | `10审计实施方案`                                     | ✅ 已实现 | 从通知书/调查导入 |
| 8 | 任务清单                         | 无                                              | ✅ 已实现 | 从方案导入 |
| 9 | 谈话记录（仅经济责任审计）          | 无                                              | ✅ 已实现 | 无 |

### 3.2 审计实施阶段（步骤 10-13）

| # | 文档名称 | 模板文件 | 实现状态 | 数据引用 |
|---|---------|---------|---------|---------|
| 10 | 审计取证单 | `11审计取证单` | ✅ 专用页面 | 从通知书导入 |
| 11 | 审计工作底稿 | `12审计工作底稿` | ✅ 专用页面 | 从取证单导入 |
| 12 | 任务清单完成情况 | `13任务清单完成情况` | ✅ 已实现 | 从任务清单导入 |
| 13 | 审计组审计报告 | 无 | ✅ 已实现 | 从调查/取证/底稿导入 |

### 3.3 审计报告阶段（步骤 14-19）

| # | 文档名称 | 模板文件 | 实现状态 | 数据引用 |
|---|---------|---------|---------|---------|
| 14 | 审计报告征求意见书 | `14审计报告征求意见书` | ✅ 已实现 | 从报告导入 |
| 15 | 审核意见书 | `15审核意见书` | ✅ 已实现 | 从征求意见书导入 |
| 16 | 复核意见书 | `16复核意见书` | ✅ 已实现 | 从审核意见导入 |
| 17 | 审理意见书 | `17审理意见书` | ✅ 已实现 | 从复核意见导入 |
| 18 | 审理会议纪要 | `19审理会议纪要` | ✅ 已实现 | 从审理意见导入 |
| 19 | 代拟稿封皮 | `18代拟稿封皮` | ✅ 已实现 | 从会议纪要导入 |

### 3.4 审计处理阶段（步骤 20-24）

| # | 文档名称 | 模板文件 | 实现状态 | 数据引用 |
|---|---------|---------|---------|---------|
| 20 | 对外报告 | `20预算执行对外报告` | ✅ 已实现 | 从报告导入 |
| 21 | 经责审计结果报告（仅经济责任审计） | 无 | ✅ 已实现 | 从对外报告导入 |
| 22 | 未在报告中反映问题清单 | 无 | ✅ 已实现 | 无 |
| 23 | 审计决定书 | 无 | ✅ 已实现 | 从对外报告导入 |
| 24 | 审计问题台账 | 无 | ✅ 已实现 | 从对外报告导入 |

### 3.5 审计归档阶段（步骤 25）

| # | 文档名称 | 模板文件 | 实现状态 | 数据引用 |
|---|---------|---------|---------|---------|
| 25 | 审计档案目录 | 无 | ✅ 已实现 | 无 |

## 四、各页面关系（来自 `各页面关系.docx`）

| # | 引用关系 | 实现状态 |
|---|--------|---------|
| 1 | 通知书：名称及落款自动生成（中共科右前旗委审计委员会办公室 科右前旗审计局），审计通知书保存后整个项目状态从草稿变为实施中，完成归档后变为已归档 | ✅ 字段已定义，通知书可编辑，后续阶段通过 `importFrom: ['notice']` 引用。审计文书送达回证日期不能早于审计通知印发日期，其他所有页面日期不能早于审计通知书日期，且必须先有通知书才能开启后续阶段工作 |
| 2 | 送达回证：文书名称和编号由审计通知书导入 | ✅ `importFrom: ['notice']`，autoImport 逻辑已实现 |
| 3 | 八不准：直接导出模板（无需编辑） | ✅ 纯导出页面，`fields: []` |
| 4 | 经责审计公示：从"公示内容"导入模板 | ✅ content 字段已定义 |
| 5 | 承诺书：直接导出模板（无需编辑） | ✅ 纯导出页面，`fields: []` |
| 6 | 调查记录：填完后自动生成模板表格，被审单位基本情况支持导入 | ✅ 独立 survey_records 表 |
| 7 | 实施方案：项目名称/组长/组员/开始日期从通知书导入 | ✅ `importFrom: ['notice', 'survey']` |
| 8 | 任务清单：从审计方案的审计内容导入 | ✅ `importFrom: ['plan']` |
| 9 | 谈话记录：直接生成模板 | ✅ 已实现 |
| 10 | 取证单：项目名称，被审单位从审计通知书导入 | ✅ `importFrom: ['notice']` |
| 11 | 审计底稿：内容从取证单导入，取证单一般涉及多个审计事项，针对取证单每一个审计事项都有一个对应的审计底稿，索引号按顺序生成如（1，2，3） | ✅ 专用页面 + evidence_working_paper_links |

## 五、模板文件标准化清单（resources/templates/）

| 编号 | 文件名 | 格式 | 占位符格式 |
|------|--------|------|-----------|
| 1 | `1经济责任审计通知书.docx` | Word | `{xxx}`（注意：Word 可能拆分为多个 run） |
| 2 | `2预算执行通知书.docx` | Word | `{xxx}` |
| 3 | `3专项审计调查通知书.docx` | Word | `{xxx}` |
| 4 | `4通知附件八不准.docx` | Word | 无（纯静态文本） |
| 5 | `5审计文书送达回证.docx` | Word | `{xxx}` |
| 6 | `6被审计单位承诺书.docx` | Word | 无（纯静态文本） |
| 7 | `7调查了解记录1基本情况表.xlsx` | Excel | `{xxx}` |
| 8 | `8调查了解记录2评估可能性.docx` | Word | `{xxx}` |
| 9 | `9调查了解记录3审计应对措施.docx` | Word | `{xxx}` |
| 10 | `10审计实施方案.docx` | Word | `{xxx}` |
| 11 | `11审计取证单.docx` | Word | `{xxx}` |
| 12 | `12审计工作底稿.docx` | Word | `{xxx}` |
| 13 | `13任务清单完成情况.xls` | Excel | `{xxx}` |
| 14 | `14审计报告征求意见书.docx` | Word | `{xxx}` |
| 15 | `15审核意见书.docx` | Word | `{xxx}` |
| 16 | `16复核意见书.docx` | Word | `{xxx}` |
| 17 | `17审理意见书.docx` | Word | `{xxx}` |
| 18 | `18代拟稿封皮.docx` | Word | `{xxx}` |
| 19 | `19审理会议纪要.docx` | Word | `{xxx}` |
| 20 | `20预算执行对外报告.docx` | Word | `{xxx}` |
| 22 | `22专项审计调查报告.docx` | Word | `{xxx}` |

**非模板文件**（保留供参考）：`各页面关系.docx`、`审计流程4.27.docx`、`说明.doc`、`13对审计报告的签证意见书.rtf`、`审计目录.xls`

## 六、界面设计规范（更新于 2026-05-28）

### 6.0 系统名称与标识

| 元素 | 设计值 |
|------|--------|
| 系统全称 | 基层审计机关审计辅助系统 v0.1 |
| 落款单位 | 科右前旗审计局 |
| Logo | `public/logo.png`（圆形国徽，48px，金色边框） |
| 版本号 | v0.1（显示在页面标题右侧+页脚） |
| 被审计对象 | 仅"被审计单位"，不含"个人"（所有页面/类型/文案已统一） |
| 页面标题 | HTML `<title>` 与 header 一致 |

### 6.1 色彩体系

| 色值 | 用途 |
|------|------|
| `#8B0000` | 主色（深红）— header 渐变、按钮背景 |
| `#B22222` | 主色（亮红）— header 渐变过渡 |
| `#FFD700` | 辅助色（金色）— header 文字、装饰线 |
| `#f5f0e8` | 背景色（暖米色）— 全局背景 |
| `#faf8f5` | 卡片内渐变起始色 |
| `#e8d5b7` | 边框色（浅棕） |

### 6.2 背景纹理与水印

全局背景使用 CSS `repeating-linear-gradient` 模拟华表柱网格纹理，所有页面通过 `body::before` 固定定位伪元素叠加华表柱水印背景。

### 6.3 页面设计清单

| 页面 | 关键组件 | 特色 |
|------|---------|------|
| App.vue | 全局header/footer | 红色渐变header，Logo+标题(v0.1)+返回首页按钮，金色装饰线 |
| Home.vue | 项目列表/搜索框/创建弹窗 | 默认显示9项+加载更多，搜索显示全部，模糊查询，网格卡片，红色弹窗 |
| ProjectDetail.vue | 阶段Tab/步骤列表 | 红色Tab导航，编号圆形徽章，步骤悬停效果 |
| StageView.vue | 路由分发器 | 分发到 GenericStageForm / UniversalStageEditor / 专用页面 |
| GenericStageForm.vue | 通用表单 | v-if/v-else 分离编辑/只读 DOM，:key 强制重建，无 readonly 属性 |
| stageConfigs.ts | 表单配置 | fields: [] 表示纯导出页面（无输入框，仅导出按钮） |

### 6.4 总体功能说明

1. 所有文件模版内将一个多个*号替换成页面输入内容，后面括号内容为注释说明当前星号的内容，在导出文件时应将对应的括号以及括号的内容剔除掉
2. 调查记录应该有三个页面（1.被审计单位基本情况表是excel表，且支持上传导入，导出完全遵照模版格式；2.评估被审计单位存在重要问题的可能性；3.确定审计事项和审计应对措施，2和3直接导出模版即可，不需要输入框，只保留导出按钮）
3. 确保所有页面的编辑/修改功能正常使用
4. 对于Excel格式模版对应页面，需要添加对应导入功能
5. 被审计单位/项目名称需要在一个项目内全局共享，即只需要在通知书填写一次
6. 不强制所有输入框都必须填写，只要修改或者输入过任何输入框内容，都允许保存
7. 注意相关文号，在导出时确保文号正常导出到模版内
8. 审计目录支持在任何位置添加行或者删除行
9. 所有导出后的日期格式遵从年月日格式如：2026年1月1日
10. 被审计单位承诺书直接导出，不需要输入框
11. 通知附件八不准直接导出，不需要输入框
12. 纯导出页面配置：`fields: []`，页面只显示标题和"导出文档"按钮

### 6.5 技术实现细节（更新于 2026-05-28）

#### 6.5.1 编辑/只读状态切换方案

**问题**：HTML `readonly` 属性在 Chrome/Edge 浏览器中动态切换时存在兼容性问题，修改 readonly 从 true 到 false 不总能恢复输入框可编辑状态。

**解决方案**（`GenericStageForm.vue` + `UniversalStageEditor.vue`）：
- `isEditing` 默认值为 `true`（首次进入即为编辑态）
- 使用 `v-if="isEditing"` / `v-else` 完全分离两个 DOM 容器
- 编辑态：`<input v-model="formData[field.key]">` 或 `<textarea>`
- 只读态：`<div class="gov-input-display">{{ displayValue(field) }}</div>`
- 配合 `:key="'edit-' + editKey"` 和 `:key="'view-' + editKey"` 强制 Vue 重建 DOM
- 点击"修改"按钮：`isEditing = true; editKey++`
- 点击"保存"成功后：`isEditing = false`

#### 6.5.2 模板占位符提取修复

**问题**：Word 经常把 `{xxx}` 占位符拆分成多个 XML run（如 `<w:t>{</w:t>` + `<w:t>xxx</w:t>` + `<w:t>}</w:t>`），直接在 XML 上正则匹配会提取到垃圾内容或遗漏占位符。

**解决方案**（`templateService.ts` — `getTemplatePlaceholders`）：
1. 先 `xml.replace(/<[^>]+>/g, '')` 去除所有 XML 标签获取纯文本
2. 再从纯文本中用正则 `/\{([^{}]+)\}/g` 匹配占位符

#### 6.5.3 模板路径解析修复

**解决方案**（`templateService.ts`）：
- 提取共享函数 `getTemplateBasePath()`，三处路径解析共用
- 开发模式优先使用 `__dirname` 相对路径，回退尝试 `app.getAppPath()`
- 增加目录模糊匹配：遍历文件名比对（处理路径偏移问题）

#### 6.5.4 日期格式化

所有导出文档中的日期字段通过 `formatDateToChinese()` 函数转换为中文格式：
- 输入：`2026-01-01` → 输出：`2026年1月1日`

## 七、审计通知书默认值配置

通知书阶段可编辑字段默认值（填写后可在其他阶段通过 `importFrom: ['notice']` 引用）：

| 字段 key | 字段名称 | 默认值 |
|----------|---------|--------|
| `auditCommitteeOffice` | 审计委员会办公室 | 中共科右前旗审计委员会办公室 |
| `auditOrg` | 审计机关全称 | 科右前旗审计局 |
| `content` | 附件 | 审计"八不准"工作纪律（含完整八条内容及举报电话） |

说明：
- `content` 字段原名"附件内容"，已更名为"附件"
- 通知书字段无只读限制，用户可自由编辑修改
- 后续阶段（如送达回证、实施方案等）通过 `importFrom: ['notice']` 自动匹配同名字段导入

## 八、纯导出页面清单

以下页面无需输入框，只显示"导出文档"按钮，直接导出模板文件：

| 步骤 key | 页面名称 | 模板文件 | 配置方式 |
|----------|---------|---------|---------|
| `eight_prohibitions` | 通知附件八不准 | `4通知附件八不准.docx` | `fields: []` |
| `commitment_letter` | 被审计单位承诺书 | `6被审计单位承诺书.docx` | `fields: []` |
| `survey_assessment` | 评估被审计单位存在重要问题的可能性 | `8调查了解记录2评估可能性.docx` | `fields: []` |
| `survey_measures` | 确定审计事项和审计应对措施 | `9调查了解记录3审计应对措施.docx` | `fields: []` |

纯导出页面的模板为静态文本（无 `{xxx}` 占位符），导出时直接复制原始文件。

## 九、核心差距总结

### 9.1 已实现情况

- **27 步全覆盖**：所有步骤均有对应的 GenericStageForm、UniversalStageEditor 或专用页面实现
- **数据引用链**：importFrom 配置已按各页面关系.docx 要求修正
- **模板命名标准化**：统一为"数字编号+中文"格式
- **审计类型约束**：仅三种（经济责任审计、预算执行审计、专项审计调查）
- **数据库适配**：trigger 已更新，匹配 27 步骤
- **完成标记**：支持阶段状态 completed 标记，含"标记完成"按钮
- **readonly 兼容性修复**：v-if/v-else DOM 分离方案，已上线 ✅
- **占位符提取修复**：先提取纯文本再匹配，解决 Word run 拆分问题 ✅
- **纯导出页面**：八不准、承诺书、评估可能性、审计应对措施已改为无输入框纯导出 ✅
- **模板格式统一**：5 个旧版 .doc 格式文件已转换为标准 .docx ✅
- **实施方案模板**：添加 {auditedUnit} 等占位符，支持自动导入被审计单位 ✅
- **基本情况表**：添加 {unitName} 占位符，支持导出时自动填入被审计单位名称 ✅

### 9.2 待修复问题

| 问题 | 描述 | 修复方案 |
|------|------|---------|
| 调查数据孤岛 | survey_records 独立表 vs stage_progress.data_json | 统一数据源或同步机制 |
| 取证/底稿自动导入断裂 | evidence_items/working_papers 独立表，stage_progress 中为空 | 重构 autoImport 支持独立表 |
| 文件附件无前端 UI | files IPC 存在但无组件 | 增加上传/管理组件 |
| 死代码文件 | StageNotice/StageSurvey/StagePlan/StageReport.vue | 删除 |

### 9.3 模板格式统一（2026-05-28）

所有模板文件必须是 `.docx`（Office Open XML）格式，部分 `.docx` 文件实际是旧版 `.doc`（OLE2 格式），已全部转换：

| 原文件 | 原格式 | 转换后 | 状态 |
|--------|--------|--------|------|
| `8调查了解记录2评估可能性.docx` | Composite Document V2 (.doc) | `.docx` | ✅ 已转换 |
| `9调查了解记录3审计应对措施.docx` | Composite Document V2 (.doc) | `.docx` | ✅ 已转换 |
| `10审计实施方案.docx` | Composite Document V2 (.doc) | `.docx` | ✅ 已转换+添加占位符 |
| `15审核意见书.docx` | Composite Document V2 (.doc) | `.docx` | ✅ 已转换 |
| `18代拟稿封皮.docx` | Composite Document V2 (.doc) | `.docx` | ✅ 已转换 |

注意：`.doc` 格式不被 docxtemplater 支持，会导致"模板文件不存在"报错（实际是 ZIP 解压失败）。
